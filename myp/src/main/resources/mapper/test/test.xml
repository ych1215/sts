<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.test.dao.TestMapper">

	<resultMap id="bordClobResultMap" type="Map">
		<result property="cont" column="cont" jdbcType="CLOB" javaType="java.lang.String" />
		<result property="seq" column="tel" />
	</resultMap>

	<select id="selectClob" parameterType="Map"	resultMap="bordClobResultMap">
		SELECT  <![CDATA[REPLACE(REPLACE(REPLACE(BORD_CLOB, CHR(13), ''), CHR(10),   '<br/>'  ), ' ', '&nbsp;') ]]>  as cont
		,seq as seq
		FROM YCH_LOGIN_INFO
		WHERE seq = ( SELECT NVL(MAX(SEQ), 1) FROM YCH_LOGIN_INFO )
	</select>
	
		<update id="insertClob" parameterType="Map">

		INSERT INTO YCH_LOGIN_INFO
		(
		SEQ,
		BORD_CLOB ,
		CREATE_DT
		)
		VALUES
		(
		(SELECT NVL(MAX(SEQ) + 1 , 1) FROM YCH_LOGIN_INFO ),
		#{clob},
		sysdate
		)

	</update>


	<select id="selectUserList" parameterType="Map" resultType="Map">
		/* tst */
		SELECT 'test' as test
		FROM dual
		WHERE 'cc'= #{aa}
	</select>


	<update id="insertUser" parameterType="Map">

		INSERT INTO YCH_LOGIN_INFO
		(
		seq
		)
		VALUES
		(
		10 / #{aa}
		)

	</update>
	


</mapper>

